[package]
name = "processing"
version = "0.1.0"
edition = "2021"
license = "MIT"
repository = "https://github.com/Zniece/YaClue"
description = "Step-by-step CAS processing layer: engine traits, expressions, step generation"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
yacas-rs = { path = "../yacas/yacas-rs" }
